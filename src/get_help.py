def help(command):
    return """Usage: inject payload.exe > destination.exe
    
    The payload is your virus and the destination is the file that you are going to inject your virus into
    
    [WARNING] Keep in mind that the payload.exe and destination.exe can have any name but they can't contain spaces and they should be in this folder together with main.py (the file you just executed)"""